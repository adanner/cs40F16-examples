#include <iostream>
#include <cmath>

/* Included just to show CMakeLists.txt syntax for
 * compiling multiple files. See that file instead */
 
using namespace std;

int main(){
  cout << "Sin(pi)= " << sin(M_PI) << endl;
  return 0;
}
